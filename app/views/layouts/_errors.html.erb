<% if obj.errors.any? %>
  <div class="error_explanation">
    <h3><%= obj.errors.count %> 件のエラーがあります:</h3>
    <ul>
      <% obj.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>