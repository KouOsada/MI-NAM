<div class="container">
  <div class="row">
    <div class="col-sm-12 col-md-8 col-lg-5 px-5 px-sm-0 mx-auto">
      <h2 class="my-4 text-center"><%= @user.nickname %>さんがフォロー中</h2>
      <% if @users.exists? %>
        <%= render 'index', users: @users %>
      <% else %>
        <p>まだフォローしていません。</p>
      <% end %>
    </div>
  </div>
</div>