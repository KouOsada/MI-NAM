<div class="row">
  <% posts.each do |postitem| %>
    <div class="card m-3">
      <%= link_to post_path(postitem.id) do %>
        <%= attachment_image_tag(postitem, :post_image) %>
      <% end %>
      <div class="card-body">
        <%= link_to post_path(postitem.id) do %>
          <%= postitem.title %>
        <% end %>
      </div>
    </div>
  <% end %>
</div>
