<!--通知が未確認の時に通知アイコンにつけるマーク-->
<%= link_to notifications_path, class: "nav-link" do %>
  <% if unchecked_notifications.any? %>
    <i class="far fa-bell"></i> 通知 (<%= unchecked_notifications.count %>)
  <% else %>
    <i class="far fa-bell"></i> 通知
  <% end %>
<% end %>